spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zy6020975/springcloudConfig
          searchPaths: config
      label: config
server:
  port: 8685
